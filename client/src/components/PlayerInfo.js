import React from "react"

const PlayerInfo = (props) => {
    return (
        <div>
            {/* each piece of info we want displayed for the player */}
        </div>
    )
}

export default PlayerInfo