import React from "react"

const Upgrades = (props) => {
    return (
        <div>
            {/* each piece of info for the upgrades that can be purchaced */}
        </div>
    )
}

export default Upgrades